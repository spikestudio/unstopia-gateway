<!-- 出力先: docs/domain/bc-{name}.md（BC ごとに 1 ファイル） -->
# BC キャンバス: [BC名]

## 責務

<!-- この BC が扱うドメイン概念と責務を 2-3 文で記述する。
     以下の観点で記述すると明確になる:
     - この BC が「何を」管理するか（扱うドメイン概念）
     - この BC が「なぜ」独立しているか（他の BC と分離する理由）
     - この BC の「境界」はどこか（扱わないもの） -->

## 集約候補

<!-- 不変条件は Epic 設計で詳細化する。ここでは集約の候補名と概要のみ。
     集約の粒度の目安:
     - 1 トランザクションで整合性を保証する単位
     - ルートエンティティを通じてのみアクセスされる
     - 他の集約への参照は ID のみ（直接参照しない） -->

| 集約名 | ルートエンティティ | 主な値オブジェクト | 備考 |
|--------|------------------|------------------|------|
| <!-- 集約名 --> | <!-- ルートエンティティ名 --> | <!-- 値オブジェクト名（カンマ区切り） --> | <!-- 補足 --> |
| <!-- 集約名 --> | <!-- ルートエンティティ名 --> | <!-- 値オブジェクト名（カンマ区切り） --> | <!-- 補足 --> |

## 主要なドメインサービス（候補）

<!-- 集約をまたぐロジック、外部連携を伴うロジック等。
     ドメインサービスの判断基準:
     - 特定の集約に属さないドメインロジック
     - 複数の集約を協調させる操作
     - 外部システムとの連携を伴う操作 -->

| サービス名 | 責務 |
|-----------|------|
| <!-- サービス名 --> | <!-- このサービスが行うこと --> |
| <!-- サービス名 --> | <!-- このサービスが行うこと --> |

## 外部依存

<!-- 他の BC や外部システムとの依存関係。
     統合パターンはコンテキストマップ（docs/domain/context-map.md）と一致させること -->

| 依存先 | 統合パターン | 備考 |
|--------|------------|------|
| <!-- BC名 or 外部システム名 --> | <!-- ACL / OHS / CF / SK / PL --> | <!-- 依存の内容・方向 --> |
| <!-- BC名 or 外部システム名 --> | <!-- ACL / OHS / CF / SK / PL --> | <!-- 依存の内容・方向 --> |
