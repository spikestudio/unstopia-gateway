# BC キャンバス: [コンテキスト名]

<!-- 境界づけられたコンテキスト（BC）の全体像を定義するドキュメント。
     Phase 定義（G1）で作成し、設計（G3）で詳細化する。
     配置先: docs/domain/bc-[slug].md -->

| 項目 | 内容 |
|------|------|
| BC 名 | <!-- コンテキスト名 --> |
| サブドメイン種別 | <!-- コア / 支援 / 汎用 --> |
| 対応 Epic | <!-- ES-NNN-slug --> |
| 責務 | <!-- この BC が責任を持つ業務領域を 1〜2 文で --> |

## ユビキタス言語（主要用語）

<!-- この BC 内で使用される主要用語。詳細は用語集（docs/glossary.md）の該当 BC セクションを参照 -->

| 用語 | 定義 | 他 BC での意味（あれば） |
|------|------|----------------------|
| <!-- 用語 --> | <!-- この BC での定義 --> | <!-- 他 BC での意味 --> |

## 主要なビジネスルール / ポリシー

<!-- 集約の不変条件に加え、BC レベルのポリシー（複数集約にまたがるルール、サーガ、リアクティブポリシー）を記述する。
     イベントストーミングの「紫の付箋（ポリシー）」に対応する -->

| ルール名 | 種別 | 説明 | 関連する集約・イベント | テスト重要度 |
|---------|------|------|---------------------|-----------|
| <!-- ルール名 --> | <!-- 不変条件 / ポリシー / サーガ --> | <!-- ルールの内容 --> | <!-- 関連する集約やイベント --> | <!-- 高 / 中 / 低 --> |

<!-- レビュー指摘: 「ポリシー」がドメインイベント一覧の「ポリシー（反応）」列と同一概念であることが不明確だった -->
<!-- 種別の区分（ドメインイベント一覧テンプレートの「ポリシー（反応）」列と対応する）:
     - 不変条件: 単一集約内で常に真であるべき制約（例: 注文合計 ≤ 100万円）
     - ポリシー: イベントに対する反応ルール（例: 注文確定 → 在庫引当）
     - サーガ: 複数集約にまたがる長期トランザクション（例: 注文→決済→配送の一連のフロー） -->

## 主要な集約

<!-- G1 段階では候補レベル。G3 でドメインモデルとして詳細化する -->

| 集約ルート | 責務 | 主要な不変条件 |
|-----------|------|-------------|
| <!-- 集約名 --> | <!-- この集約が管理するビジネスルール --> | <!-- 常に真であるべき制約 --> |

## 入力イベント（この BC が受け取るイベント）

| イベント名 | 発行元 BC | この BC での反応（ポリシー） |
|-----------|----------|------------------------|
| <!-- イベント名 --> | <!-- 発行元 --> | <!-- 何をするか --> |

## 出力イベント（この BC が発行するイベント）

| イベント名 | トリガー（コマンド） | 受信先 BC |
|-----------|-------------------|----------|
| <!-- イベント名 --> | <!-- 何が起きたら発行するか --> | <!-- 誰が受け取るか --> |

## 統合パターン

<!-- この BC と他の BC の統合方式 -->

| 相手 BC | 方向 | 統合パターン | 通信方式 | 備考 |
|--------|------|------------|---------|------|
| <!-- BC名 --> | <!-- 上流→下流 / 下流→上流 --> | <!-- ACL / OHS / 共有カーネル / コンフォーミスト / 顧客-供給者 / イベント駆動 / 別々の道 / パートナーシップ / 公開言語 --> | <!-- REST API / メッセージキュー / gRPC / DB共有 --> | <!-- 補足 --> |

## 外部依存

<!-- この BC が依存する外部システム・サービス -->

| 外部システム | 用途 | 統合方式 |
|------------|------|---------|
| <!-- システム名 --> | <!-- 何に使うか --> | <!-- API / SDK / ファイル連携 --> |
