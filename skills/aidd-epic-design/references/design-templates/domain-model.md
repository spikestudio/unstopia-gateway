<!-- 出力先: docs/domain/[epic-slug]-domain-model.md -->
# 集約モデル詳細 — [Epic名]

> Phase のドメイン分析で特定された集約候補を詳細化する。
> 不変条件・属性・値オブジェクト・ドメインイベントのペイロードをここで定義する。

## 集約一覧

| 集約名 | ルートエンティティ | 責務（1文） |
|--------|------------------|------------|
| [集約名] | [ルートエンティティ名] | [この集約が保証する整合性の範囲] |
| [集約名] | [ルートエンティティ名] | [この集約が保証する整合性の範囲] |

<!-- 以下、集約ごとに H3 セクションを作成する -->

### [集約名]

#### 不変条件

<!-- この集約が常に満たすべきビジネスルール。
     トランザクション境界内で保証される制約を列挙する -->

- [不変条件1: 例「注文金額は 0 より大きい」]
- [不変条件2: 例「ステータスが確定済みの場合、明細の変更は不可」]

#### エンティティ属性

| 属性名 | 型 | 制約 | 説明 |
|--------|-----|------|------|
| [属性名] | [型] | [NOT NULL / UNIQUE / FK 等] | [この属性の意味] |
| [属性名] | [型] | [NOT NULL / UNIQUE / FK 等] | [この属性の意味] |

#### 値オブジェクト

<!-- 値オブジェクトの判断基準:
     - 同一性ではなく属性の等価性で比較される
     - 不変（immutable）である
     - 自己検証ロジックを持つ -->

| 名前 | 属性 | バリデーション |
|------|------|--------------|
| [値オブジェクト名] | [属性（カンマ区切り）] | [バリデーションルール] |
| [値オブジェクト名] | [属性（カンマ区切り）] | [バリデーションルール] |

#### ドメインサービス（該当する場合）

<!-- 集約をまたぐロジック、外部連携を伴うロジック等。
     特定の集約に属さないドメインロジックがある場合のみ記載する -->

| サービス名 | 責務 | 関連集約 |
|-----------|------|---------|
| [サービス名] | [このサービスが行うこと] | [関連する集約名（カンマ区切り）] |

## ドメインイベント詳細

> Phase で定義済みのイベント名にペイロードを追加する。
> Phase 定義: `docs/domain/domain-events.md`

| イベント名 | ペイロード | 発行条件 |
|-----------|----------|---------|
| [イベント名（過去形）] | [ペイロードのフィールド（カンマ区切り）] | [どの操作・状態遷移で発行されるか] |
| [イベント名（過去形）] | [ペイロードのフィールド（カンマ区切り）] | [どの操作・状態遷移で発行されるか] |
