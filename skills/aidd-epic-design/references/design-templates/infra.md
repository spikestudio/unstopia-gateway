<!-- 出力先: docs/design/[epic-slug]-infra.md -->
# インフラ設計骨格 — [Epic名]

> 骨格レベルの定義: 新設・変更するリソース一覧・デプロイ構成・環境差異まで。
> 詳細なパラメータ（インスタンスサイズ・IOPS・タイムアウト値等）は Task で定義する。

## 対象スコープ

このEpicでインフラに影響する変更の概要:

[例: 新規マイクロサービス追加 / 既存サービスのスケールアウト / 外部サービス連携追加 等]

## クラウドリソース一覧

| リソース種別 | 名前/識別子 | 操作 | 用途 | 対応AC |
|------------|-----------|------|------|--------|
| [DB / Queue / Storage / Container 等] | [名前] | [新設 / 変更 / 削除] | [役割] | [AC-NNN] |

## デプロイアーキテクチャ

```
[コンポーネント図を Mermaid で記述]
graph LR
    A[Client] --> B[API Gateway]
    B --> C[Service A]
    C --> D[(Database)]
```

## 環境ごとの構成差異

| 項目 | dev | stg | prod | 備考 |
|------|-----|-----|------|------|
| [リソース名] | [設定値] | [設定値] | [設定値] | [理由] |

<!-- 例: DBインスタンス数 | 1 | 1 | 2 (HA) | 本番のみ冗長化 -->

## 外部依存・連携サービス

| サービス名 | 用途 | 認証方式 | タイムアウト方針 | 障害時フォールバック | SLA要件 |
|-----------|------|---------|----------------|-------------------|--------|
| [外部API/SaaS名] | [用途] | [APIキー / OAuth 等] | [タイムアウト後の挙動] | [例外スロー / デフォルト値返却 / キャッシュ利用] | [可用性要件] |

## 非機能要件との対応

| 非機能要件 | 対応方針 |
|-----------|---------|
| 可用性 | [冗長化・フェイルオーバーの方針] |
| スケーラビリティ | [オートスケール等の方針] |
| セキュリティ | [ネットワーク分離・アクセス制御の方針] |

<!-- 非機能要件の目標値は Phase 定義書 §11 を参照 -->
