# Task: [ES-027] Story 全件 — ES-027 E2E 検証（src/mcp ブランチカバレッジ 90% 以上確認）

| 項目 | 内容 |
|------|------|
| 例外承認 Issue | — |
| Issue | #184 |
| Epic 仕様書 | ES-027 |
| Story | S1, S2, S3, S4, S5, S6, S7 |
| Complexity | S |
| PR | #TBD |

## 責務

全実装 Task（TASK-060〜065）完了後に `pnpm test --coverage` を実行し、`src/mcp` モジュール全体のブランチカバレッジが 90% 以上であることを確認する。全 42 AC が少なくとも 1 つのテストでカバーされていることを最終検証する。

## スコープ

対象ファイル:

- 変更なし（検証のみ）

対象外（隣接 Task との境界）:

- 全実装 Task（TASK-060〜065）が完了していること

## Epic から委ねられた詳細

- **デモシナリオ**: `pnpm test --filter=jimmy --coverage` を実行し、出力に `src/mcp/gateway-server.ts` と `src/mcp/resolver.ts` のブランチカバレッジが 90% 以上と表示されること
- **カバレッジ確認方法**: vitest の coverage レポート（c8 または v8）で `Branch` 列を確認する
- **未カバー AC の対処**: ブランチカバレッジが 90% 未満の場合、対応テストを TASK-060〜065 のいずれかに追記してカバレッジを達成する
- **テスト実行時間要件**: 全テスト（`src/mcp` のみ）が 30 秒以内に完了すること（Epic 仕様書 §非機能要件）
- **ネットワーク不要の確認**: `fetch` モックで全テストが実行されること（ネットワークアクセスなし）

## 完了条件

**機能面（AC-ID 参照）:**

- [ ] **AC 全 42 件**: TASK-060〜065 で実装した全テストが PASS している
- [ ] **非機能要件カバレッジ**: `src/mcp` モジュール全体のブランチカバレッジが 90% 以上
- [ ] **非機能要件テスト時間**: 全テストが 30 秒以内に完了
- [ ] **デモシナリオ確認**: `pnpm test --filter=jimmy --coverage` でカバレッジレポートが出力される
- [ ] Epic 仕様書の全 AC チェックボックス更新（AC-E027-01〜42 全件）

### 品質面

- [ ] コードレビューが承認されている
- [ ] CI パイプラインがグリーン

## テスト方針

| テストレイヤー | 対象 | 備考 |
|-------------|------|------|
| ユニットテスト | 該当なし（既存テスト PASS 確認のみ） | — |
| ドメインロジックテスト | 該当なし | — |
| 統合テスト | 該当なし | — |
| E2E テスト | AC-E027-01〜42 全件: `pnpm test --coverage` でカバレッジ 90% 以上確認 | ローカル環境で実行 |

**検証手順:**

```bash
# 1. テスト全件 PASS 確認
pnpm test --filter=jimmy

# 2. カバレッジ確認（src/mcp のブランチカバレッジ 90% 以上）
pnpm test --filter=jimmy --coverage

# 3. テスト実行時間確認（30 秒以内）
# vitest の出力で各テストの実行時間を確認
```

## AI への指示コンテキスト

| 項目 | 内容 |
|------|------|
| BC（境界づけられたコンテキスト） | 該当なし（テスト専用 Epic） |
| サブドメイン種別 | 支援: E2E 検証 |

- 参照 Epic 仕様書: ES-027 §E2E 検証計画、§非機能要件（ブランチカバレッジ 90%・テスト実行 30 秒以内）
- 参照設計: `docs/domain/mcp-test-coverage-ac-mapping.md` §全件チェック

## 依存

- 先行 Task: TASK-060, TASK-061, TASK-062, TASK-063, TASK-064, TASK-065（全 Task 完了後）

## 引き渡し前チェック

- [ ] 完了条件が全て検証可能な形で記述されている
- [ ] 対応する Epic AC（全 42 件）が特定され、完了条件と対応づけられている
- [ ] 参照すべき Epic 仕様書・ADR・既存コードが「AI への指示コンテキスト」に記載されている
- [ ] 参照設計にセクション番号/名（§）が記載されている
- [ ] コンテキスト量が複雑度レベルの目安（S: ~200 行）に収まっている
- [ ] 規約ドキュメント群にこの Task で使う規約・パターンが記載されている
- [ ] 先行 Task（TASK-060〜065 全て）が完了しコードがマージ済みである
- [ ] ドキュメントに書かれていない暗黙の要件がない
- [ ] Epic から委ねられた詳細が転記されている
