# Task: [ES-028] E2E 検証 — stt テストカバレッジ最終確認

| 項目 | 内容 |
|------|------|
| 例外承認 Issue | — |
| Issue | #193 |
| Epic 仕様書 | ES-028 |
| Story | S1-S6 (全ストーリー) |
| Complexity | S |
| PR | #<!-- Step 2 で記入 --> |

## 責務

TASK-067〜072 で実装した全テストを実行し、`src/stt/stt.ts` のブランチカバレッジが 90% 以上であることを確認する。全テストが PASS することと、CI がグリーンであることを最終確認する。

## スコープ

対象ファイル:

- `packages/jimmy/src/stt/__tests__/stt.test.ts`（読み取りのみ、変更なし）

対象外:

- `src/stt/stt.ts` 本体の変更（テスト追加 Epic のため実装変更なし）

## Epic から委ねられた詳細

- 該当なし（検証専用 Task）

## 完了条件

**機能面（AC-ID 参照）:**

- [ ] **全 AC カバレッジ**: AC-E028-01〜25 の全 25 件が対応するテストで PASS していること
- [ ] **ブランチカバレッジ 90% 以上**: `pnpm test -- --coverage` を実行し、`src/stt/stt.ts` のブランチカバレッジが 90% 以上であることを coverage レポートで確認すること
- [ ] Epic 仕様書（`docs/requirements/ES-028-stt-test-coverage.md`）の全 AC チェックボックスが更新されていること

### 品質面

- [ ] `pnpm test` で全テストが PASS している
- [ ] `pnpm lint` がクリーン（biome-ignore 禁止）
- [ ] `pnpm typecheck` がクリーン
- [ ] CI パイプラインがグリーン

## テスト方針

| テストレイヤー | 対象 | 備考 |
|-------------|------|------|
| ユニットテスト | TASK-067〜072 の全テスト通過確認 | vitest で実行 |
| ドメインロジックテスト | 該当なし | |
| 統合テスト | 該当なし | |
| E2E テスト | `pnpm test -- --coverage` でブランチカバレッジ 90% 以上 | デモシナリオ: coverage レポートの確認 |

**検証手順:**

```bash
# 1. 全テスト実行
pnpm test

# 2. カバレッジレポート生成
pnpm test -- --coverage

# 3. src/stt/stt.ts のブランチカバレッジが 90% 以上であることを確認
```

## AI への指示コンテキスト

| 項目 | 内容 |
|------|------|
| BC（境界づけられたコンテキスト） | 該当なし（インフラ/ユーティリティ層） |
| サブドメイン種別 | 支援: テスト追加 |

- 参照 Epic 仕様書: ES-028 全ストーリー（S1〜S6）E2E 検証計画セクション
- 参照設計: 該当なし
- 参照 ADR: 該当なし
- 参照コード:
  - `packages/jimmy/src/stt/__tests__/stt.test.ts`（TASK-067〜072 で作成したテストファイル）
  - `packages/jimmy/src/stt/stt.ts`（カバレッジ対象）

## 依存

- 先行 Task: TASK-067, TASK-068, TASK-069, TASK-070, TASK-071, TASK-072（全テスト実装 Task の完了が前提）

## 引き渡し前チェック

- [ ] 完了条件が全て検証可能な形で記述されている
- [ ] 対応する Epic の全 AC が特定され、完了条件と対応づけられている
- [ ] 参照すべき Epic 仕様書・ADR・既存コードが「AI への指示コンテキスト」に記載されている
- [ ] 参照設計にセクション番号/名（§）が記載されている
- [ ] コンテキスト量が複雑度レベルの目安（S: ~200 行）に収まっている
- [ ] 規約ドキュメント群にこの Task で使う規約・パターンが記載されている
- [ ] 先行 Task が完了しコードがマージ済みである（TASK-067〜072 全件完了が前提）
- [ ] ドキュメントに書かれていない暗黙の要件がない
- [ ] Epic から委ねられた詳細が転記されている（該当なし）
