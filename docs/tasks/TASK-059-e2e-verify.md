# Task: [ES-026] Story 8 — E2E 検証（pnpm build && pnpm test 全 PASS + カバレッジ確認）

| 項目 | 内容 |
|------|------|
| 例外承認 Issue | — |
| Issue | #169 |
| Epic 仕様書 | ES-026 |
| Story | 8 |
| Complexity | S |
| PR | #TBD |

## 責務

リファクタリング完了後に `pnpm build && pnpm test` が全 PASS することを確認し、既存動作が維持されていることを保証する。すべての AC が少なくとも 1 つのテストでカバーされていることを最終確認する。

## スコープ

対象ファイル:

- 変更なし（検証のみ）

対象外（隣接 Task との境界）:

- 全実装 Task（TASK-051〜058）が完了していること

## Epic から委ねられた詳細

- **デモシナリオ**: `pnpm test --coverage` で `gateway/api/` の branch カバレッジが 100% と表示される。各ファイルが 500 行以下であることを `wc -l` で確認する（AC-E026-19 の最終検証）
- `pnpm build` がエラーなく完了すること（型エラー・インポートエラーなし）
- `pnpm test` が全テスト PASS すること（既存テストの破損なし・新規テストも全 PASS）
- 検証環境: ローカル開発環境（データベース不要・全モック）
- 前提条件: `pnpm install` 完了。Node.js / biome / vitest が利用可能

## 完了条件

**機能面（AC-ID 参照）:**

- [ ] **AC-E026-24**: `pnpm build` がエラーなく完了する（型エラー・インポートエラーなし）
- [ ] **AC-E026-25**: `pnpm test` が全テスト PASS する（既存テストの破損なし・新規テストも全 PASS）
- [ ] **AC-E026-22 最終確認**: `pnpm test --coverage` で `gateway/api/` 以下の branch カバレッジが 100%
- [ ] **AC-E026-19 最終確認**: `wc -l packages/jimmy/src/gateway/api/*.ts` で全ファイルが 500 行以下
- [ ] Epic 仕様書の全 AC チェックボックス更新（AC-E026-01〜25 全件）

### 品質面

- [ ] コードレビューが承認されている
- [ ] CI パイプラインがグリーン

## テスト方針

| テストレイヤー | 対象 | 備考 |
|-------------|------|------|
| ユニットテスト | 該当なし（既存テスト PASS 確認のみ） | — |
| ドメインロジックテスト | 該当なし | — |
| 統合テスト | 該当なし | — |
| E2E テスト | AC-E026-24, 25: `pnpm build && pnpm test` 全 PASS | ローカル環境で実行 |

**検証手順:**

```bash
# 1. ビルド確認
pnpm build

# 2. テスト全件 PASS 確認
pnpm test

# 3. カバレッジ確認
pnpm test --coverage

# 4. ファイルサイズ確認
wc -l packages/jimmy/src/gateway/api/*.ts
```

## AI への指示コンテキスト

| 項目 | 内容 |
|------|------|
| BC（境界づけられたコンテキスト） | 該当なし（支援ドメイン） |
| サブドメイン種別 | 支援: 標準設計 + 統合テスト中心 |

- 参照 Epic 仕様書: ES-026 Story 8（AC-E026-24〜25）+ E2E 検証計画セクション
- 参照設計: `docs/design/session-runner-sessions-refactor-api-spec.md` §Section 6（AC カバレッジ確認）

## 依存

- 先行 Task: TASK-051, TASK-052, TASK-053, TASK-054, TASK-055, TASK-056, TASK-057, TASK-058（全 Task 完了後）

## 引き渡し前チェック

- [ ] 完了条件が全て検証可能な形で記述されている
- [ ] 対応する Epic AC（全 AC）が特定され、完了条件と対応づけられている
- [ ] 参照すべき Epic 仕様書・ADR・既存コードが「AI への指示コンテキスト」に記載されている
- [ ] 参照設計にセクション番号/名（§）が記載されている
- [ ] コンテキスト量が複雑度レベルの目安（S: ~200行）に収まっている
- [ ] 規約ドキュメント群にこの Task で使う規約・パターンが記載されている
- [ ] 先行 Task（TASK-051〜058 全て）が完了しコードがマージ済みである
- [ ] ドキュメントに書かれていない暗黙の要件がない
- [ ] Epic から委ねられた詳細が転記されている
